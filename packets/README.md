# Packets

Each Stage 1 run writes its dated packet here, plus job-packets-today.md which Stage 2 reads.

If a run produces zero jobs it writes ONLY the dated file and leaves job-packets-today.md alone, so an unrun Stage 2 does not lose yesterday.
